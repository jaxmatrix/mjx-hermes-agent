{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capabilities for Hermes Universal. The webview makes no network calls itself — all HTTP/WS traffic runs in Rust and is surfaced via events + IPC commands. It also creates no windows: every window kind (session, instance, tile, screen, satellite) is built by a Rust command, so the label a window is opened under is never a value from the webview (MJXHRM-382). core:webview:allow-create-webview-window is deliberately absent and must not be re-added — it takes an arbitrary label and URL, which is what made the sat-* namespace forgeable. The `webviews` globs below ARE the scope for every permission here: window.rs opens exactly main / instance-<n> / tile-<slug> / sat-<surface> / screen, and a label outside that list gets no IPC at all. They are scoped by WEBVIEW rather than by window (MJXHRM-447) and that is a security boundary, not a spelling change: Tauri resolves a command with `cmd.webviews.matches(webview) || cmd.windows.matches(window)`, so a `windows` glob would hand the app's entire permission set to the in-app browser's CHILD WEBVIEW, which lives inside window `main`. For every surface that exists today the two are identical — a WebviewWindow builds its window and its webview under one label — but the guest's label is `guest:<id>`, which matches no glob here and must never be made to. src/browser/capability_globs.rs is the test that keeps it that way. Credential storage is deliberately absent from this list: it is no longer a plugin. src/secrets/ exposes secrets_get/set/delete/clear/status, whose key parameter is a Rust enum rather than the arbitrary account string the old keyring plugin took — which any window in the globs below could use to read any entry, including the nativeAuth:<gateway> OAuth token sets Rust owns. Do NOT re-add a generic credential command. For clipboard-manager that failure is silent — the reject lands in the same catch as 'plugin absent' in @/lib/clipboard and degrades to the web API — so the list must be kept in step with window.rs.",
  "webviews": ["main", "session-*", "instance-*", "tile-*", "sat-*", "screen"],
  "permissions": [
    "core:default",
    "core:event:default",
    "os:allow-platform",
    "mic:allow-check-permission",
    "mic:allow-request-permission",
    "opener:allow-open-url",
    "haptics:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read-file",
    "fs:allow-write-text-file",
    "notification:default",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-image",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-set-position",
    "core:window:allow-start-dragging",
    "core:window:allow-is-maximized",
    "core:webview:allow-set-webview-zoom"
  ]
}
