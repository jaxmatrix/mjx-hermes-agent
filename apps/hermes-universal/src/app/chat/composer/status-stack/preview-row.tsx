// STUB — FIXME(chat-port): the desktop PreviewStatusRow surfaces localhost /
// on-disk preview artifacts recorded per session (preview-status store), opening
// them in the browser via window.hermesDesktop.openPreviewInBrowser. Universal's
// preview store (@/store/preview) is a tab model, not a per-session artifact
// feed, so there is nothing to render here yet. Kept for structural parity. FLAG.
export function PreviewStatusRow() {
  return null
}
