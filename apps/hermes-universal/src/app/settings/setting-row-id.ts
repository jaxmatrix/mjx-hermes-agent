/** DOM ids for ⌘K settings-search deep links. Kept off AUTO `settings-search.ts`. */
export const settingRowElementId = (id: string): string => `setting-row-${id}`

export const credentialRowElementId = (key: string): string => `credential-row-${key}`
