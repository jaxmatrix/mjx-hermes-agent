/**
 * Extra icons used by universal host surfaces that desktop's `lib/icons`
 * barrel does not re-export. Keep this protected — do not fold into AUTO
 * `icons.ts` or the next absorb will wipe it.
 */
export {
  IconChevronUp as ChevronUp,
  IconLayoutGrid as LayoutGrid,
  IconMenu2 as Menu,
  IconPaw as Paw,
  IconSparkles as Sparkles,
  IconStars as Stars,
  IconWebhook as Webhook
} from '@tabler/icons-react'
